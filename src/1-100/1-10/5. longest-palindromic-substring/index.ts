/**
 * 5. 最长回文子串 (Longest Palindromic Substring)
 * 难度：5/10
 * 链接：https://leetcode.cn/problems/longest-palindromic-substring/
 *
 * 任务摘要：
 * 在给定字符串中找到长度最大的回文连续片段并返回该片段。
 *
 * 说明：
 * - 合法答案可能不唯一
 * - 返回任一最长回文子串即可
 */
export function longestPalindrome(s: string): string {
  return ''
}
