/**
 * 6. Z 字形变换 (Zigzag Conversion)
 * 难度：3/10
 * 链接：https://leetcode.cn/problems/zigzag-conversion/
 *
 * 任务摘要：
 * 将字符串按指定行数走“之字形”写入后，再按行拼接得到新字符串。
 *
 * 说明：
 * - 行数为 1 时结果与原串相同
 * - 重点在于字符落点顺序与读取顺序
 */
export function convert(s: string, numRows: number): string {
  return ''
}